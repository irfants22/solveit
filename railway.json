{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "composer install --no-dev --optimize-autoloader && npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan storage:link && php artisan config:cache && php -S 0.0.0.0:8080 -t public/"
  }
}
